// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("fontkit", "for fontkit support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("fontkit", "enable fontkit support", "no");

if (PHP_FONTKIT != "no") {
	EXTENSION("fontkit", "fontkit.c", PHP_EXTNAME_SHARED, "/DZEND_ENABLE_STATIC_TSRMLS_CACHE=1");
}

